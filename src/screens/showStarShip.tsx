import React from 'react'
import { View, Text } from 'react-native'

interface Props {

}

const showStarShip = (props: props) => {
  const {error, isLoading, data} = showStarShip
if isLoading{


  return (
    <View>
      <Text>ggg</Text>
    </View>
  )
  }
}

export default showStarShip

